# Build and Test Artifacts

This runbook captures the build and verification commands for each EduLense target after the final polish pass completed on March 30, 2026.

## Verified Checks

### Root web app

```bash
cd /Users/kwameduodu/EduLense/EduLense
npm run build
npm run lint
npm test -- --run
```

Status:

- Production build passes
- Lint passes
- Vitest suite passes

### Electron desktop app

```bash
cd /Users/kwameduodu/EduLense/EduLense/edulence_desktop
npm run build
npm run lint
npm test
```

Status:

- Renderer build passes
- Lint passes
- Jest suite passes

### React Native app

```bash
cd /Users/kwameduodu/EduLense/EduLense/edulence_rn
npm run typecheck
npm test
```

Status:

- TypeScript typecheck passes
- Jest and snapshot suite passes

### Flutter app

```bash
cd /Users/kwameduodu/EduLense/EduLense/edulence
flutter test
```

Status:

- Widget and accessibility tests pass

## Build Output Locations

### Root web app

```bash
cd /Users/kwameduodu/EduLense/EduLense
npm run build
```

Output:

- `dist/`

### Electron desktop app

```bash
cd /Users/kwameduodu/EduLense/EduLense/edulence_desktop
npm run build
```

Renderer output:

- `edulence_desktop/dist/`

Packaged desktop artifacts:

```bash
npm run dist
```

Output:

- Generated by `electron-builder` in the default release output directory for the platform

### React Native app

Android release build:

```bash
cd /Users/kwameduodu/EduLense/EduLense/edulence_rn
npm run build:android:release
```

iOS release build:

```bash
cd /Users/kwameduodu/EduLense/EduLense/edulence_rn
npm run build:ios:release
```

Artifacts:

- Downloaded from the EAS build page printed by the CLI

### Flutter app

Android APK:

```bash
cd /Users/kwameduodu/EduLense/EduLense/edulence
flutter build apk --release
```

Output:

- `edulence/build/app/outputs/flutter-apk/app-release.apk`

iOS archive / IPA:

```bash
flutter build ipa --release
```

Output:

- Signed IPA: `edulence/build/ios/ipa/*.ipa`
- Unsigned archive when using `--no-codesign`: `edulence/build/ios/archive/Runner.xcarchive`

## Notes

- React Native release builds depend on valid Expo/EAS credentials.
- Flutter iOS release builds depend on local signing configuration.
- The verified commands above were run locally during the March 30, 2026 polish pass; cloud build submission commands were documented but not executed in this pass.
